import React, { use } from 'react';

const pricingOption = ({promisData}) => {
    const prisingData = use(promisData);
    console.log(prisingData)
    return (
        <div>
            
        </div>
    );
};

export default pricingOption;